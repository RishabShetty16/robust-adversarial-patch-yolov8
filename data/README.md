# Dataset

This project uses the COCO dataset for digital adversarial patch optimization.

Expected directory structure:

```
data/
└── coco/
    ├── images/
    └── labels/
```

The dataset is **not included** in this repository because of its size.

Images and labels should be placed manually before training.